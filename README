# ProjetcRendaApp

DEPENDECIES: 

IONIC STORAGE: npm i @ionic/storage-angular (Caso não instale essa dependecia o Storage não irá funcionar, instale na raiz do projeto)

IONIC: npm i -g @ionic/cli (Comando necessário para instalar o Ionic)

JQUERY: npm install @types/jquery(Comando necessário para instalar o jQuery)

import * as $ from 'jquery' (Importar no "App.module")

ionic integrations enable capacitor (Comando para poder habilitar as funções do capacitor)

ionic capacitor add android (Comando para adicionar o android ao projeto)

Ng-Circle-Progress: npm install ng-circle-progress --save (Comando para os graficos na Dashboard)

npm install @ionic/pwa-elements (Comando para usar a camera da Web)

import {defineCustomElements} from '@ionic/pwa-elements/loader' (Definir no arquivo main.ts)

defineCustomElements(window)(Definir no arquivo main.ts)


